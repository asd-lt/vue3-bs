services:
  app:
    image: node:20-slim
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    # Keep the container running so we can exec commands or use 'docker compose run'
    command: tail -f /dev/null
